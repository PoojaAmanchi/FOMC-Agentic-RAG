langchain==0.3.7
langchain-community==0.3.5
langchain-google-genai==2.0.7
langchain-huggingface==0.1.2
langchain-text-splitters==0.3.2
langgraph==0.2.53
chromadb==0.5.18
sentence-transformers==3.2.1
# ragas 0.4+ uses a new API: EvaluationDataset/SingleTurnSample instead of
# HuggingFace Dataset, and metrics are instantiated with the LLM directly.
# See eval/run.py and eval/ablation.py for the updated usage.
# ragas 0.4 also uses Google's OpenAI-compatible endpoint for Gemini, so
# the openai package is required even though we use Gemini as the backend.
ragas==0.4.3
openai>=1.0.0
datasets>=4.0.0
python-dotenv==1.0.1
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pandas==2.2.3
